---
all:
  children:
    hardened:
      hosts:
        # Uncomment and set your box. Play 1 connects with the user you
        # currently have (cloud default, e.g. `ubuntu`), Play 2+ reconnects
        # as {{ admin_user }}.
        #
        # my-server:
        #   ansible_host: 10.0.0.10
        #   ansible_user: ubuntu
